<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cart_nav_graph"
    app:startDestination="@id/cartFragment">

    <fragment
        android:id="@+id/cartFragment"
        android:name="com.example.sub10.vistas.HomeCliente.CartFragment"
        android:label="fragment_cart"
        tools:layout="@layout/cliente_fragment_cart">

        <action
            android:id="@+id/action_cartFragment_to_pedidoEditFragment"
            app:destination="@id/pedidoEditFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />

    </fragment>

    <fragment
        android:id="@+id/pedidoEditFragment"
        android:name="com.example.sub10.vistas.HomeCliente.PedidoEditFragment"
        android:label="fragment_edit_pedido"
        tools:layout="@layout/cliente_fragment_editar_pedido">

        <argument
            android:name="nameQuery"
            app:argType="string"
            app:nullable="false" />

    </fragment>
</navigation>
